"use strict";

/*
const checkManaStamina = (character, actionTaken) => {

    //console.log('DEBUG mana: ', character.props.mana_points);
    //console.log('DEBUG mana cost: ', actionTaken.props.mana_points_cost);
    //console.log('DEBUG stamina: ', character.props.stamina_points);
    //console.log('DEBUG stamina cost: ', actionTaken.props.stamina_points_cost);

    if (!character.checkMana(actionTaken.props.mana_points_cost) && !character.checkStamina(actionTaken.props.stamina_points_cost)){
        return {
            "text": `_You don't have enough mana or stamina to use ${actionTaken.props.name}_`
        }
    }
    if (!character.checkMana(actionTaken.props.mana_points_cost)){
        return {
            "text": `_You don't have enough mana to use ${actionTaken.props.name}_`
        }
    }
    if (!character.checkStamina(actionTaken.props.stamina_points_cost)){
        return {
            "text": `_You don't have enough stamina to use ${actionTaken.props.name}_`
        }
    }
};

module.exports = {
    checkManaStamina
};*/